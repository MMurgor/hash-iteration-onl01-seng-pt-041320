# passengers = {
# suite_a: "Amanda Presley", 
# suite_b: "Seymour Hoffman", 
# suite_c: "Alfred Tennyson", 
# suite_d: "Charlie Chaplin", 
# suite_e: "Crumpet the Elf"
# }

def select_winner(passengers)
  passengers.each do |suite_a, |"Amanda Presley"|
  if suite == :suite_a && name.start_with?("A")
    winner = name
end
end

def select_winner(passengers)
  passengers.each do |suite_b, |"Seymour Hoffman"|
  if suite == :suite_b && name.start_with?("B")
    winner = name
  end
end

def select_winner(passengers)
  passengers.each do |suite_c, |"Alfred Tennyson"|
  if suite == :suite_c && name.start_with?("C")
    winner = name
  end
end


def select_winner(passengers)
  passengers.each do |suite_d, |"Charlie Chaplin"|
  if suite == :suite_d && name.start_with?("D")
    winner = name
  end
end
  

def select_winner(passengers)
  passengers.each do |suite_e, |"Crumpet the Elf"|
  if suite == :suite_e && name.start_with?("E")
    winner = name
  end
end 
